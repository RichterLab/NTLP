#!/bin/bash

#$ -M hpark6
#$ -m n
#$ -pe mpi-16 64
#$ -q *@@richter
#$ -N E+L-UMabl
#$ -j y

case_data=mabl
case_home=mabl
time=0000000
runout=$case_data.out.$time

datadir=/scratch365/hpark6/mabl/$case_data
homedir=~/NTLP/$case_home

cd $homedir

module load mvapich2
module load intel

imachine=0
echo $imachine > ./mach.file
echo $datadir >> ./mach.file

mpirun -n 64 $homedir/../build/lesmpi.a $homedir/params.in > $datadir/$runout


