#!/bin/bash

#$ -M drichte2@nd.edu
#$ -m n
#$ -pe mpi-16 32
#$ -q *@@richter,*@@ivybridge
#$ -N prof1
#$ -j y

case=prof1
time=0000000
runout=$case.out.$time

datadir=/scratch365/drichte2/profiles/$case
homedir=~/Research/profiles/$case

cd $homedir

module load mvapich2/2.1-intel-15.0-mlx
module load intel/15.0

imachine=0
echo $imachine > ./mach.file
echo $datadir >> ./mach.file

mpirun -n 32 $homedir/../build/lesmpi.a $homedir/params.in > $datadir/$runout


